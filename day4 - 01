<?php

  $fr = "bgvyzdsv";

  $x = 0;
  $answ = false;

  do {
      $md  = md5($fr.$x);

      if (substr($md, 0, 5)==="00000") {$answ = $x;}
      else $x++;

  } while ($answ==false);

  echo $answ;

?>
